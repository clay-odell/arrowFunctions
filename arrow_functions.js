const double = arr => arr.map(val => val * 2);

console.log(double([1, 2, 3])); //just to demonstrate the code works

const squareAndFindEvens = (numbers) => squares = numbers.map(val => val **2).filter(square => square % 2 === 0);
console.log(squareAndFindEvens ([7, 12, 64])); //test code to see if it works.
